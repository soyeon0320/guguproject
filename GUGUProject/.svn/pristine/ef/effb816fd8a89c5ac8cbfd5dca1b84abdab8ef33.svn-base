package service;

import java.util.List;

import vo.BoardManagerVO;

public interface BoardManagerService {
	
	/**
	 * 통합 검색을 위한 메서드
	 * @param searchName 사용자로 부터 받은 검색어를 DB에서 비교 한다.
	 * @return 검색된 데이터를 list에 담아 반환한다.
	 */
	public List<BoardManagerVO> searchList(String searchName);	
	
	
}
